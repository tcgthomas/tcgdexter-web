/**
 * Default render for the `@modal` slot when no intercepted route is
 * active. Returning `null` means the slot is empty and the list renders
 * by itself.
 */
export default function ModalDefault() {
  return null;
}
